# with_interaction

最小交互示例。

## 兼容点

- 原命令 `with_interaction` 仍可触发。
- `interaction_entries` 声明交互 Bot 入口。
- `on_interaction()` 返回结构化 `result` / `settlement`。

## 结构

```text
with_interaction/
├── __init__.py
├── plugin.json
├── manifest.py
├── plugin.py
└── README.md
```
