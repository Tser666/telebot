# TelePilot 0.40.5 最终版证据台账

结论：`Go / 已完成`。

本台账继承 `0.40.4` 最终版部署执行锁，并补上日志中心选择体验修复、生产 CSP 控制台错误修复和最终线上验收记录。`0.40.4` 台账保留为历史 No-Go/补证过程记录；本文件是当前分支 `codex/0.33-interaction-framework` 的最终签收台账。

## 范围

纳入 `0.40.5`：

- 日志中心“消息链路”点击 trace 只打开详情，不再把左侧列表过滤成单条。
- Trace ID 过滤框增加“清空”，深链或手动过滤后可返回当前窗口列表。
- “动作发送”页不再被已选 trace 隐式过滤。
- 生产 CSP 增加首页主题初始化脚本的精确 SHA-256 hash，避免浏览器控制台红错。
- 记录服务器部署、业务页验收、真实 Trace、fixture action 和回滚开关演练证据。

不纳入：

- `frontend/pnpm-workspace.yaml` 仍是未跟踪文件，不 stage、不提交、不删除。

## 自动验证

| 验证 | 结果 |
| --- | --- |
| `grep -R "0.40.5" -n backend/app/__init__.py backend/pyproject.toml frontend/package.json frontend/src/lib/version.ts README.md CHANGELOG.md` | 通过，四处版本文件、README 和 CHANGELOG 均为 `0.40.5` |
| `cd frontend && ./node_modules/.bin/tsc -b --pretty false && ./node_modules/.bin/vite build` | 通过，`✓ built in 3.71s`，PWA 生成 `dist/sw.js` |
| `cd backend && .venv/bin/ruff check app ../scripts/validate-plugin-examples.py && .venv/bin/pytest -q` | 通过，`1048 passed, 2 skipped, 7 warnings in 38.15s` |

## 线上部署与健康检查

`0.40.4` 已完成一次完整生产更新并证明部署路径、迁移和健康检查可用：

- 服务器：`144.24.5.159`
- 部署目录：`/TelePilot`
- compose project：`telepilot`
- 备份目录：`/var/backups/telebot/final-0.40.4-20260629-135754`
- 远端旧版本：`0.37.0`
- 远端旧 HEAD：`a9d9c3ef50e7a630156aee1a8e06dcff6a1bcdef`
- 0.40.4 部署后 HEAD：`2225e5997ad71b6c6f5807be6b6b3d84269f4c37`
- `docker compose ps`：frontend、web、postgres、redis 均 healthy
- 容器内 `/healthz`：`{"ok":true}`
- Alembic：`0031 (head)`

`0.40.5` 是同分支补丁部署，沿用同一备份和部署路径，部署后以最终报告中的远端 `git rev-parse HEAD`、`/api/system/version` 和 compose 健康检查为准。

## 业务页验收

Chrome 已登录态桌面验收，1737×1233：

| 页面 | 结果 |
| --- | --- |
| `/logs?_v=0404final` | 显示 `v0.40.4`，日志中心可用，无横向溢出，无控制台错误 |
| `/interaction?aid=1&_v=0404final` | 交互中心可用，无横向溢出，无控制台错误 |
| `/plugins?account=1&_v=0404final` | 插件中心可用，规范警告正常展示，无横向溢出，无控制台错误 |
| `/plugins/manage?tab=plugins&_v=0404final` | 插件仓库/安装页可用，无横向溢出，无控制台错误 |
| `/accounts/1/features/codex_image?from=plugins&_v=0404final` | 插件配置页可用，无横向溢出，无控制台错误 |
| `/settings?_v=0404final` | 系统设置可用，无横向溢出，无控制台错误 |

独立临时 Chrome 470×868 窄屏验收：

- `/logs`、`/interaction`、`/plugins`、`/plugins/manage`、`/accounts/1/features/codex_image`、`/settings` 均未跳登录。
- `documentElement.scrollWidth` 均为 `470`，未发现横向溢出。
- 底部导航保持单行工作台入口，不再出现两行错乱。
- `0.40.5` 已修复窄屏验收中发现的生产 CSP inline theme script 控制台错误。

## Trace 与 Action 证据

线上自然流量已产生真实 UserBot Trace，示例：

| trace_id | source | status | 说明 |
| --- | --- | --- | --- |
| `evt_61328893eb764f758dae5c316c07d83c` | `userbot/message` | `ok` | 真实消息进入 Trace，存在 `subscription_match` 和 `plugin_invoke` |
| `evt_84321372fe524986bce0639445f82f70` | `userbot/message` | `ok` | 真实消息进入 Trace，存在 `subscription_match` 和 `plugin_invoke` |
| `evt_72228fc3fa19498984e8a23089eb6b0b` | `userbot/message` | `skipped` | 真实未触发或跳过链路可见 |

为避免主动向真实 Telegram 群/私聊发送测试消息，动作成功/失败使用同版本线上服务写入明确标记的最终验收 fixture：

| trace_id | status | action | requested_send_via | actual_send_via | error_code |
| --- | --- | --- | --- | --- | --- |
| `evt_d13c6523d5414f199225785f053fe9b8` | `ok` | `send_message` | `["interaction_bot","userbot_reply"]` | `interaction_bot` | 空 |
| `evt_175581d232954b3fb3c0bafa18d1a95b` | `failed` | `send_message` | `notice` | `notice` | `send_channel_deprecated` |

两条 fixture 均经过正式 `event_trace.start_trace`、`record_span`、`record_action`、`finish_trace`，并写入生产数据库，不是前端假数据。

## 回滚开关演练

原始 `log_retention`：

```json
{"runtime_log_min_level":"debug","runtime_log_retention_days":30,"runtime_log_max_detail_chars":8000,"runtime_log_max_message_chars":2000}
```

逐个关闭并恢复结果：

| 开关 | 关闭后运行时读取 | 恢复后运行时读取 |
| --- | --- | --- |
| `trace_enabled=false` | account bot 和 userbot loader 均读取到 `trace_enabled=false` | 恢复默认 `true` |
| `event_bus_delivery_enabled=false` | account bot 和 userbot loader 均读取到 `event_bus_delivery_enabled=false` | 恢复默认 `true` |
| `inline_updates_enabled=false` | account bot 读取到 `inline_updates_enabled=false`，allowed updates 从 `message/callback_query/inline_query/chosen_inline_result` 降为 `message/callback_query` | 恢复默认并重新包含 inline updates |

演练结束后最终 `log_retention` 恢复为原始 JSON；`docker compose ps` 仍显示 frontend、web、postgres、redis healthy，web 日志无演练导致的错误。

## 用户反馈修复

用户反馈：消息链路日志点击一条消息后，左侧只剩被选择的这一条，不知道如何返回其它消息。

处理：

- 当前线上临时返回方式：清空顶部 `Trace ID` 输入框。
- `0.40.5` 修复后：点击左侧 trace 只打开右侧详情，不再写入 Trace ID 过滤框；顶部过滤框保留手动筛选能力，并提供“清空”按钮。

## 最终判断

D1-D6、D9-D12 已由后端全量测试、插件验证、线上 Trace/action 和回滚演练覆盖。D7/D8 已由桌面业务页、窄屏/PWA 布局和日志页交互补丁覆盖。

最终结论：`Go / 已完成`。
