# TelePilot 0.40.4 最终版证据台账

结论：`No-Go / 阻塞`。

本台账对应当前分支 `codex/0.33-interaction-framework` 的 `0.40.4` 封口工作区。当前代码、文档、示例、前端构建和扩展后的最终版门禁回归测试已达到本地可测状态；在业务页登录态、服务器 `144.24.5.159` 部署、真实 trace 和回滚演练补齐前，不得宣称最终版 `Go / 已完成`。

## G0 事实冻结

复验时间：`2026-06-29 13:37:13 CST`。

| 项目 | 当前事实 | 处理 |
| --- | --- | --- |
| 分支 | `codex/0.33-interaction-framework`，跟踪 `origin/codex/0.33-interaction-framework` | 继续在当前分支封口，不覆盖 `main` |
| 本轮起点 | 本轮开始时本地 HEAD 与远端同为 `d9065046b39e2ad49ecc20c73cd4a8453ee0e8ce` | 已完成 `0.40.3` 发布检查点提交并推送；本轮继续形成 `0.40.4` 最终版部署执行锁检查点 |
| 版本 | `backend/app/__init__.py`、`backend/pyproject.toml`、`frontend/package.json`、`frontend/src/lib/version.ts` 均更新为 `0.40.4` | 发布材料按 `0.40.4 patch（补丁版本）` 处理 |
| 纳入发布 | 第 31 节最终版交付包、服务器部署目录探测、线上真实链路触发剧本、失败回滚剧本、插件开发指南终审清单、README 当前版本修正、中文 `CHANGELOG.md`、本台账 | 用于后续 G0-G6 签收和部署前检查 |
| 不纳入发布 | `frontend/pnpm-workspace.yaml` | 当前仍是未跟踪占位文件；不 stage、不提交、不删除 |
| 服务器版本 | `curl -fsS https://telebot.260505.xyz/api/system/version` 返回 `{"version":"0.37.0","stage":null}` | 服务器尚未部署当前分支，D11 不能签收 |

## D1-D12 签收状态

| 编号 | 状态 | 当前证据 | 剩余风险 |
| --- | --- | --- | --- |
| D1 所有主入口有 Trace | 可测 | 扩展目标 pytest 覆盖 UserBot、管理员命令、交互 Bot message/callback、inline_query、chosen_inline_result、payment、auto_award；全量 pytest 通过 | 仍需线上真实 trace 展示 |
| D2 所有插件调度经过 Event Bus decision 或 rule_bound decision | 可测 | 扩展目标 pytest 覆盖插件命令、交互 message/callback、inline/chosen、session_close、payment、auto_award decision | 仍需日志页 matched/skipped/delivered 业务页展示 |
| D3 插件开发主路径只依赖标准事件信封与 MessageOps | 可测 | `validate-plugin-examples.py` 通过；文档主路径为 Event Bus + Trace + MessageOps；第 30/31 节补充签收与收束执行口径 | 第三方旧插件仍需迁移 |
| D4 `native_raw` 只对显式声明插件下发 | 可测 | 能力测试覆盖 boolean true 拒绝、对象 `enabled=true` + source gate；全量 pytest 通过 | 仍需业务页风险提示验收 |
| D5 旧 `notice` / `bbot_notice` / `notice_bot` 只失败不发送 | 可测 | `send_channel_deprecated` 相关测试、示例 fixture、远程/已安装插件验证脚本通过 | 仍需旧 notice 失败线上 trace |
| D6 所有用户可见动作成功/失败落 `event_action` | 可测 | 扩展目标 pytest 覆盖 Delivery、scheduler、helper、callback、inline answer、settlement/auto_award、账号 Bot 系统通知和消息模板测试发送；全量 pytest 通过 | 仍需线上动作失败 trace |
| D7 日志页能解释收到、未触发、插件失败、动作失败和 fallback | 半落地 | 日志页源码字段与后端 API 对齐；前端类型和生产构建通过 | 缺已登录 `/logs` 桌面和窄屏/PWA 业务页证据 |
| D8 交互中心、插件中心、仓库、插件配置页能展示订阅、能力、风险和更新 | 半落地 | 插件仓库展开列表已补 usage、订阅、能力和风险提示；前端类型和生产构建通过 | 缺已登录业务页、长文本、空态、错误态和窄屏/PWA 证据 |
| D9 开发文档不推荐旧主路径 | 可测 | 旧概念 grep 已执行；命中按迁移说明、废弃说明、测试 fixture、兼容层和存量插件债分类；示例验证通过 | 存量第三方插件仍有 usage/event_subscriptions 迁移 warning |
| D10 版本、CHANGELOG、证据台账、commit、远端分支一致 | 半落地 | 四处版本、README、中文 CHANGELOG 和本台账已更新到 `0.40.4`；本地自动门禁通过 | 仍需中文 commit、push 后核对本地/远端 SHA；后续如再改证据台账，必须重新提交、推送并核对 |
| D11 服务器完成备份、迁移、健康检查、真实 trace 验收 | 未开始 | 当前服务器版本仍为 `0.37.0` | 本机仍需可用 SSH/等价部署通道，部署前必须备份 |
| D12 三个回滚开关关闭和恢复演练 | 可测 | 目标测试覆盖 `event_bus_delivery_enabled=false` 时付款通知不进入 Event Bus、`trace_enabled=false` 时 UserBot Event Bus 仍投递但不写 Trace、scheduler 不创建 Trace / 不写 `event_action`、系统设置写回；D1-D6/D12 目标测试组和全量 pytest 通过 | 缺线上关闭前值、关闭后表现、恢复后值和降级日志 |

## 自动验证

以下命令均在 `0.40.4` 当前工作区执行过：

| 命令 | 结果 |
| --- | --- | --- |
| `cd backend && .venv/bin/ruff check app ../scripts/validate-plugin-examples.py && .venv/bin/pytest -q app/tests/test_account_bot.py::test_interaction_plain_message_routes_to_worker_entry_as_message app/tests/test_account_bot.py::test_interaction_callback_routes_to_worker_entry_and_answers_callback app/tests/test_account_bot.py::test_interaction_inline_query_routes_through_event_bus_and_records_trace app/tests/test_account_bot.py::test_interaction_chosen_inline_result_routes_through_event_bus app/tests/test_account_bot.py::test_interaction_delivery_executor_answer_inline_query_records_success_and_failure app/tests/test_account_bot.py::test_notify_account_records_trace_action app/tests/test_account_bot_auto_award.py::test_account_bot_auto_award_records_trace_action app/tests/test_worker_command.py app/tests/test_plugin_security_regression.py app/tests/test_scheduler_runtime.py app/tests/test_plugin_loader.py app/tests/test_system_settings.py app/tests/test_message_templates.py` | 通过，`149 passed, 1 warning in 1.48s` |
| `cd backend && .venv/bin/pytest -q` | 通过，`1048 passed, 2 skipped, 7 warnings in 39.51s` |
| `backend/.venv/bin/python scripts/validate-plugin-examples.py` | 通过；`event_bus_demo`、`with_ai`、`with_http`、`with_interaction` ok；`translate` 跳过 |
| `backend/.venv/bin/python scripts/validate-installed-interaction-plugins.py` | 通过但保留旧插件 warning：`dice_grid_hunt`、`guess_number`、`lottery_plus`、`poetry_blank`、`pt_promote`、`redpack-byRBQ` 缺 usage 或仍以旧 `interaction_entries` 为主 |
| `cd backend && .venv/bin/alembic heads && .venv/bin/alembic upgrade head --sql >/tmp/telepilot-alembic-0.40.4-final.sql && wc -l /tmp/telepilot-alembic-0.40.4-final.sql` | 通过；`0031 (head)`，offline SQL 873 行 |
| `cd frontend && ./node_modules/.bin/tsc -b --pretty false` | 通过，无输出 |
| `cd frontend && ./node_modules/.bin/vite build` | 通过，`✓ built in 3.71s`，PWA 生成 `dist/sw.js` |
| 旧概念审计：`rg -n "notice\|bbot_notice\|notice_bot\|raw_event\|payload\\[\"text\"\\]\|event\\.reply\|event\\.respond\|ctx\\.client\|旧规则驱动" README.md docs examples scripts backend frontend >/tmp/telepilot-old-concepts-0.40.4.txt; wc -l /tmp/telepilot-old-concepts-0.40.4.txt` | 已执行，910 行命中；命中按迁移说明、废弃说明、测试 fixture、兼容层、存量插件债和自动回复内部 notice 文案分类，不作为新插件推荐主路径 |
| 新契约覆盖审计：`rg -n "event_subscriptions\|capabilities\|telegram_native_raw\|native_raw_meta\|answer_inline_query\|chosen_inline_result\|settlement\|send_channel_deprecated" README.md docs backend frontend examples scripts >/tmp/telepilot-new-contracts-0.40.4.txt; wc -l /tmp/telepilot-new-contracts-0.40.4.txt` | 已执行，969 行命中；README、插件开发文档、示例、验证脚本、后端 schema/runtime 和前端页面均覆盖最终版契约关键词 |
| `git diff --check` | 通过，无输出 |

## 浏览器验收

| 页面/视口 | 结果 |
| --- | --- |
| in-app browser：`https://telebot.260505.xyz/logs?_v=0404-g3`，1280×720 | 待部署 `0.40.4` 后复验；未登录页面或旧版本页面不能签收 G3 |
| Chrome：`https://telebot.260505.xyz/logs`，1737×1289 | 已知有线上登录态，但当前线上仍显示旧版本；部署 `0.40.4` 后必须重新验收 |
| `/logs`、`/interaction?aid=1`、`/plugins`、`/plugins/manage?tab=plugins`、真实插件配置页、`/settings` | 需在部署 `0.40.4` 后重新进行桌面和窄屏/PWA 业务页验收，D7/D8 目前不能签收 |

## 远端部署

| 检查 | 结果 |
| --- | --- |
| `curl -fsS https://telebot.260505.xyz/api/system/version` | 返回 `{"version":"0.37.0","stage":null}`，服务器尚未部署当前分支 |
| `curl -I -sS https://telebot.260505.xyz/` | 返回 `HTTP/1.1 200 OK`，`Last-Modified: Sat, 27 Jun 2026 19:33:37 GMT`，仍是旧静态部署 |
| 远端 SSH | 待按第 31.11 节用可用密钥复验真实 compose working_dir、备份路径和部署命令 | 不能假设 `/opt/telepilot` |

部署前必须先从 Docker compose label 或第 31.11 节探测流程确认真实部署目录。若部署目录确认后项目脚本可用，优先执行：

```bash
cd /opt/telepilot
./deploy/backup.sh
TELEPILOT_UPDATE_BRANCH=codex/0.33-interaction-framework make prod-update
```

部署后必须补齐：远端 commit、版本 API、`docker compose ps`、`docker compose logs --tail=100 web`、健康检查、真实 trace_id、三个回滚开关关闭/恢复记录。

## Go / No-Go

当前结论保持 `No-Go / 阻塞`，原因不是本地代码验证失败，而是最终版门禁缺证据：

- D7/D8：Chrome 有线上登录态，但服务器仍是 `0.37.0`；缺部署当前分支后的业务页桌面和窄屏/PWA 验收。
- D11：服务器 `144.24.5.159` 当前仍是 `0.37.0`。
- D12：本地代码和测试已覆盖关键关闭分支，但缺线上关闭并恢复 `trace_enabled`、`event_bus_delivery_enabled`、`inline_updates_enabled` 的演练证据。

补齐以上证据后，才能按计划第 30/31 节把结论升级为 `Go / 已完成`。
