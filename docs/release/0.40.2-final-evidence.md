# TelePilot 0.40.2 最终版证据台账

结论：`No-Go / 阻塞`。

本台账对应当前分支 `codex/0.33-interaction-framework` 的 `0.40.2` 封口工作区。当前代码、文档、示例和构建已达到本地可测状态，但最终版 `Go / 已完成` 仍缺四类外部证据：已登录业务页验收、服务器 `144.24.5.159` 部署、真实 trace、回滚开关演练。缺证据前不得宣称最终版已上线。

## F0 事实冻结

| 项目 | 当前事实 | 处理 |
| --- | --- | --- |
| 分支 | `codex/0.33-interaction-framework`，跟踪 `origin/codex/0.33-interaction-framework` | 继续在当前分支封口，不覆盖 `main` |
| 提交基线 | 本轮开始时本地 HEAD 为 `fbcb44b332fafb7db1882f47480af8c17d561cad`，远端同分支也指向该提交 | 本轮 `0.40.2` 提交形成后需在最终报告列出实际 SHA |
| 版本 | `backend/app/__init__.py`、`backend/pyproject.toml`、`frontend/package.json`、`frontend/src/lib/version.ts` 均更新为 `0.40.2` | 发布材料按 `0.40.2 patch（补丁版本）` 处理 |
| 纳入发布 | 第 29 节最终版执行封条、四处版本号、中文 `CHANGELOG.md`、本台账 | 用于封口计划和证据同步 |
| 不纳入发布 | `frontend/pnpm-workspace.yaml` | 文件内容仍是 `esbuild: set this to true or false` 占位；不 stage、不提交 |

## D1-D12 签收状态

| 编号 | 状态 | 当前证据 | 剩余风险 |
| --- | --- | --- | --- |
| D1 所有主入口有 Trace | 可测 | 后端定向测试覆盖 UserBot、管理员命令、交互 Bot message/callback、payment、auto_award；全量 pytest 通过 | 仍需线上真实 trace 展示 |
| D2 所有插件调度经过 Event Bus decision 或 rule_bound decision | 可测 | `test_dispatch_plugin_command_creates_event_bus_decision`、交互 message/callback、session_close、payment、auto_award 目标测试通过 | 仍需日志页展示 matched/skipped/delivered |
| D3 插件开发主路径只依赖标准事件信封与 MessageOps | 可测 | `validate-plugin-examples.py` 通过；文档主路径为 Event Bus + Trace + MessageOps；子 Agent 只读复核 D3 已证明 | 第三方旧插件仍需迁移 |
| D4 `native_raw` 只对显式声明插件下发 | 可测 | boolean true 被拒绝、对象 `enabled=true` + source gate 测试通过；子 Agent 只读复核 D4 已证明 | 线上需观察风险提示展示 |
| D5 旧 `notice` / `bbot_notice` / `notice_bot` 只失败不发送 | 可测 | `send_channel_deprecated` 相关测试和示例 fixture 通过；子 Agent 只读复核 D5 已证明 | `transfer_test_notice` 是测试消息路径，不属于旧三值发送通道；需产品侧保持边界清晰 |
| D6 所有用户可见动作成功/失败落 `event_action` | 可测 | Delivery、scheduler、helper、callback、inline、settlement/auto_award 相关测试通过；子 Agent 只读复核 D6 已证明 | 仍需线上动作失败 trace |
| D7 日志页能解释收到、未触发、插件失败、动作失败和 fallback | 半落地 | 源码字段与后端 API 对齐；前端类型和构建通过 | 新浏览器会话被 `/login` 守卫拦截，缺已登录 `/logs` 桌面和窄屏/PWA 证据 |
| D8 交互中心、插件中心、仓库、插件配置页能展示订阅、能力、风险和更新 | 半落地 | 源码使用真实后端字段；前端类型和构建通过；子 Agent 未发现明显创造字段 | 缺已登录业务页、长文本、空态、错误态和窄屏/PWA 证据 |
| D9 开发文档不推荐旧主路径 | 可测 | 旧概念 grep 已分类；子 Agent 只读复核 D9 已证明 | 旧插件文档命中只能保留为迁移/兼容说明 |
| D10 版本、CHANGELOG、证据台账、commit、远端分支一致 | 半落地 | 四处版本和中文 CHANGELOG 已更新到 `0.40.2` | 本轮提交和 push 完成前不能签收；最终 SHA 需在最终报告列出 |
| D11 服务器完成备份、迁移、健康检查、真实 trace 验收 | 未开始 | `https://telebot.260505.xyz/api/system/version` 当前返回 `0.37.0` | SSH 到 `root/ubuntu/opc/debian@144.24.5.159` 均 `Permission denied`，无法备份和部署 |
| D12 三个回滚开关关闭和恢复演练 | 证据不足 | 代码和测试覆盖 `trace_enabled`、`event_bus_delivery_enabled`、`inline_updates_enabled` 关闭分支；子 Agent 判断代码可验证 | 缺线上关闭前值、关闭后表现、恢复后值和降级日志 |

## 自动验证

以下命令均在 `0.40.2` 工作区运行过：

| 命令 | 结果 |
| --- | --- |
| `cd backend && .venv/bin/ruff check app/services/account_bot_runtime.py app/services/account_bot_service.py app/services/interaction/delivery.py app/worker/command.py app/worker/runtime.py app/worker/plugins/loader.py app/worker/plugins/sandbox.py app/worker/scheduler_runtime.py app/tests/test_account_bot.py app/tests/test_account_bot_auto_award.py app/tests/test_worker_command.py app/tests/test_plugin_security_regression.py app/tests/test_scheduler_runtime.py` | 通过，`All checks passed!` |
| D1-D6 定向 pytest：`cd backend && .venv/bin/pytest -q ... app/tests/test_plugin_security_regression.py app/tests/test_scheduler_runtime.py` | 通过，`85 passed, 1 warning in 1.13s` |
| `cd backend && .venv/bin/pytest -q` | 通过，`1038 passed, 2 skipped, 7 warnings in 36.87s` |
| `backend/.venv/bin/python scripts/validate-plugin-examples.py` | 通过；`event_bus_demo`、`with_ai`、`with_http`、`with_interaction` ok；`translate` 跳过 |
| `backend/.venv/bin/python scripts/validate-installed-interaction-plugins.py` | 通过但保留旧插件 warning：`dice_grid_hunt`、`guess_number`、`lottery_plus`、`poetry_blank`、`pt_promote`、`redpack-byRBQ` 缺 usage 或仍以旧 `interaction_entries` 为主 |
| `cd backend && .venv/bin/alembic heads && .venv/bin/alembic upgrade head --sql >/tmp/telepilot-alembic-current-final.sql && wc -l /tmp/telepilot-alembic-current-final.sql` | 通过；`0031 (head)`，offline SQL 873 行 |
| `git diff --check` | 通过，无输出 |
| `cd frontend && ./node_modules/.bin/tsc -b --pretty false` | 通过，无输出 |
| `cd frontend && ./node_modules/.bin/vite build` | 通过，`✓ built in 3.86s`，PWA 生成 `dist/sw.js` |

## 浏览器验收

| 页面/视口 | 结果 |
| --- | --- |
| `https://telebot.260505.xyz/`，1280×720 | 新浏览器会话被重定向到 `/login`；登录页无横向溢出 |
| `/logs`、`/interaction?aid=1`、`/plugins`、`/plugins/manage?tab=plugins`、真实插件配置页、`/settings` | 缺已登录远端业务页证据，D7/D8 不能签收 |

## 远端部署

| 检查 | 结果 |
| --- | --- |
| `curl -fsS https://telebot.260505.xyz/api/system/version` | 返回 `{"version":"0.37.0","stage":null}`，服务器尚未部署当前分支 |
| `curl -I -sS https://telebot.260505.xyz/` | 返回 `HTTP/1.1 200 OK`，但静态资源仍是旧部署 |
| `git ls-remote origin refs/heads/codex/0.33-interaction-framework` | 本轮开始时远端为 `fbcb44b332fafb7db1882f47480af8c17d561cad`；本轮提交后需重新核对 |
| `ssh root@144.24.5.159` | `Permission denied (publickey,password)` |
| `ssh ubuntu@144.24.5.159` | `Permission denied (publickey,password)` |
| `ssh opc@144.24.5.159` | `Permission denied (publickey,password)` |
| `ssh debian@144.24.5.159` | `Permission denied (publickey,password)` |

部署命令仍应按文档执行：

```bash
cd /opt/telepilot
./deploy/backup.sh
TELEPILOT_UPDATE_BRANCH=codex/0.33-interaction-framework make prod-update
```

部署后必须补齐：远端 commit、版本 API、`docker compose ps`、`docker compose logs --tail=100 web`、健康检查、真实 trace_id、三个回滚开关关闭/恢复记录。

## Go / No-Go

当前结论保持 `No-Go / 阻塞`，原因不是本地代码验证失败，而是最终版门禁缺外部证据：

- D7/D8：缺已登录远端业务页桌面和窄屏/PWA 验收。
- D10：本轮提交和远端 push 完成前仍未闭合。
- D11：服务器 `144.24.5.159` 当前仍是 `0.37.0`，且本机 SSH 无权限。
- D12：缺线上关闭并恢复 `trace_enabled`、`event_bus_delivery_enabled`、`inline_updates_enabled` 的演练证据。

补齐以上证据后，才能按计划第 29 节把结论升级为 `Go / 已完成`。
